import { Box } from '@mui/system';

export default function MapApi() {
  return (
    <Box
      sx={{
        height: '23rem',
        padding: '3vh 3vh 3vh 3vh',
        maxHeight: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        flexDirection: 'column',
      }}
    >
      <p>대충 지도 api 들어갈 자리</p>
    </Box>
  );
}
