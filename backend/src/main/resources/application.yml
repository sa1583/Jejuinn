spring:
  datasource:
    url: jdbc:mysql://database-tamra.cryffwvzpqtt.ap-northeast-2.rds.amazonaws.com:3306/tamnaradb?serverTimezone=UTC&useSSL=false
    username: admin
    password: tamnarapwd
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create # auto table drop
    properties:
      hibernate:
        # show_sql: true
        format_sql: true
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging.level:
  org.hibernate.SQL: debug
# org.hibernate.type: trace

# swagger setting
springfox:
  documentation:
    swagger:
      v2:
        use-model-v3: false

